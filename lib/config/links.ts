/**
 * Canonical external links used across the site.
 *
 * Centralizing the GitHub URL keeps every "view source / contribute"
 * affordance (top bar, hero, footer, about page) pointing at the same
 * canonical repository.
 */

/** Public source repository for CommonGrid. */
export const GITHUB_URL = "https://github.com/TextureHQ/commongrid";
